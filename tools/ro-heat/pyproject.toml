[project]
name = "ro-heat"
version = "1"
requires-python = "==3.12.*"
dependencies = [
    "pandas",
    "sqlalchemy",
    "PyYAML",
    "psycopg2-binary",
    "entise==1.1.0",
    "infdb",
]

[tool.uv.sources]
infdb = { path = "/workspaces/infdb_package", editable = true }
